{{#each posts}}
  <div class="post">
    <h2><a href="/post/{{id}}">{{title}}</a></h2>
    <p>{{content}}</p>
    <p>Created by: {{User.username}}</p>
    <p>Created on: {{format_date createdAt}}</p>
    <hr>
  </div>
{{else}}
  <p>No posts found.</p>
{{/each}}
